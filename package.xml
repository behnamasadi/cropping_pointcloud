<?xml version="1.0"?>
<package format="3">
  <name>cropping_pointcloud</name>
  <version>1.0.0</version>
  <description>
    Crops an incoming sensor_msgs/PointCloud2 against an axis-aligned bounding
    box (six tunable bounds) and optionally extracts the dominant non-planar
    object via RANSAC plane removal. ROS 2 port with native parameters
    replacing dynamic_reconfigure.
  </description>
  <maintainer email="behnam.asadi@gmail.com">Behnam Asadi</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>rclcpp</depend>
  <depend>sensor_msgs</depend>
  <depend>pcl_conversions</depend>
  <depend>libpcl-all-dev</depend>

  <exec_depend>ros2launch</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
